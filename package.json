{
  "name": "shiftwise",
  "version": "1.0.0",
  "description": "Advanced store scheduling for managers: employees, availability, auto-scheduling, reports.",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "serve": "python3 -m http.server 8080"
  }
}
